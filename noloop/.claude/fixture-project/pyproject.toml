[project]
name = "fixture-adw"
version = "0.1.0"
description = "Banco de pruebas del método ADW no-loop: contiene errores deliberados"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]
