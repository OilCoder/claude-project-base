{
  "schema_version": 1,
  "plan_id": "compose-pipeline",
  "objective": "Implement alpha, beta, and gamma so that the unit tests pass.",
  "base_revision": "REPLACED",
  "final_verification": { "commands": ["python3 -m unittest discover -s tests"] },
  "phases": [
    {
      "phase_id": "core",
      "objective": "Independent leaf functions",
      "depends_on": [],
      "contracts": [
        {
          "contract_id": "alpha",
          "objective": "Implement alpha",
          "work_class": "localized-low-risk-code-change",
          "risk": "low",
          "read": ["lib/alpha.py", "tests/test_alpha.py"],
          "allowed_to_modify": ["lib/alpha.py"],
          "forbidden": ["tests/**", "lib/beta.py", "lib/gamma.py"],
          "requirements": ["alpha doubles its input"],
          "verification": { "commands": ["python3 -m unittest tests/test_alpha.py"], "invariants": ["Only lib/alpha.py changes"] },
          "budgets": { "max_builder_attempts": 2, "max_contract_revisions": 0, "max_unplanned_scope_expansion": 0 },
          "response": ["status", "changed files"]
        },
        {
          "contract_id": "beta",
          "objective": "Implement beta",
          "work_class": "localized-low-risk-code-change",
          "risk": "low",
          "read": ["lib/beta.py", "tests/test_beta.py"],
          "allowed_to_modify": ["lib/beta.py"],
          "forbidden": ["tests/**", "lib/alpha.py", "lib/gamma.py"],
          "requirements": ["beta adds three to its input"],
          "verification": { "commands": ["python3 -m unittest tests/test_beta.py"], "invariants": ["Only lib/beta.py changes"] },
          "budgets": { "max_builder_attempts": 2, "max_contract_revisions": 0, "max_unplanned_scope_expansion": 0 },
          "response": ["status", "changed files"]
        }
      ]
    },
    {
      "phase_id": "compose",
      "objective": "Composition depends on both leaves",
      "depends_on": ["core"],
      "contracts": [
        {
          "contract_id": "gamma",
          "objective": "Implement gamma",
          "work_class": "localized-low-risk-code-change",
          "risk": "low",
          "read": ["lib/gamma.py", "tests/test_gamma.py"],
          "allowed_to_modify": ["lib/gamma.py"],
          "forbidden": ["tests/**", "lib/alpha.py", "lib/beta.py"],
          "requirements": ["gamma returns beta(alpha(value))"],
          "verification": { "commands": ["python3 -m unittest tests/test_gamma.py"], "invariants": ["Only lib/gamma.py changes"] },
          "budgets": { "max_builder_attempts": 1, "max_contract_revisions": 0, "max_unplanned_scope_expansion": 0 },
          "response": ["status", "changed files"]
        }
      ]
    }
  ]
}
